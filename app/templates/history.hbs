<div class="container">
    {{#each model.matches as |match|}}
        {{sc2-match current=match CurrentPlayer=model.CurrentPlayer}}
    {{/each}}
    {{outlet}}
    {{#link-to "new-match"}}
    <button class="btn btn-info pull-left">
        <strong><span class="glyphicon glyphicon-plus"></span></strong>
        &nbsp;&nbsp;
        New Match
    </button>
    {{/link-to}}
</div>
